// src/types/express/index.d.ts or similar
// import "express";
// import { User } from "@/db/types.ts";

// declare global {
//   namespace Express {
//     interface UserExpiry extends User {
//       expiry?: number; // ⬅️ Extend the existing `User`, not create a new one
//     }
//   }
// }